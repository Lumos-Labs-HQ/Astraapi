version: '3.8'

services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    deploy:
      resources:
        limits:
          cpus: '0.1'
          memory: 512M
    restart: unless-stopped
